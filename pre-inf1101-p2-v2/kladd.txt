TODO:
Inverted Index: 
- Må mappe ord til hashmap, og så en lenket liste/hashmap med filnavn til ordet. 

- Formel for score kan være antall ganger i documentet som parse

- Må lage egen parser.c fil
    - se bnf (Backus-Naur Form)


# Index.c:
    - 
    - **Program to search for words (terms) in text documents**  
    Builds an in-memory index of terms from a given directory of files, providing a command line interface to search for words across all parsed documents.
